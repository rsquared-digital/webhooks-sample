const root = __dirname;
const public = [__dirname, '.public'].join('/');
const client = [__dirname, 'client'].join('/');
const server = [__dirname, 'server'].join('/');

module.exports = {
  root,
  public, 
  client,
  server
}